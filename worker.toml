name = "bc-trackvision"
main = "./_worker.js"
compatibility_date = "2025-01-15"

# Serve the static portfolio assets via the bundled Worker.
assets = { directory = ".", binding = "ASSETS", html_handling = "auto-trailing-slash", not_found_handling = "single-page-application" }

[observability]
enabled = true

