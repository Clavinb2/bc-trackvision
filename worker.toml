name = "bc-trackvision"
main = "./_worker.js"
compatibility_date = "2025-01-15"

# Serve the static portfolio assets via the bundled Worker.
assets = { directory = ".", binding = "ASSETS", html_handling = "always_on", not_found_handling = "404" }

[observability]
enabled = true

